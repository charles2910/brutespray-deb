#!/usr/bin/make -f

#No setup.py available in sources, will push a merge request
override_dh_auto_clean:
	[ ! -e setup.py ] || dh_auto_clean

#Building manpages
override_dh_auto_build:
	dh_auto_build
	ronn -r $(CURDIR)/debian/brutespray.1.ronn

%:
	dh $@ --with python2 --buildsystem=pybuild
